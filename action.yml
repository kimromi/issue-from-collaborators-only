name: 'kimromi/protect-issues'
description: 'Provide a description here'
author: 'Your name or organization here'
inputs:
  github_token:
    description: 'GitHub token'
    required: true
  protected_users:
    description: 'Set GitHub username that does not delete the issues'
    required: true
runs:
  using: 'node16'
  main: 'dist/index.js'
